import { SessionsMessenger } from "@/components/client-sessions/sessions-messenger";

export default function ClientSessionsPage() {
  return (
    <div className="h-screen bg-background">
      <SessionsMessenger />
    </div>
  );
}
